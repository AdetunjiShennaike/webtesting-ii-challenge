import React from 'react';
import { render, fireEvent } from 'react-testing-library'

import AtBat from './batterUp'

describe('batter testing', () => {
  it('tests', () => {
    render(<AtBat />)
  })
})